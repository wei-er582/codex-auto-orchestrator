interface:
  display_name: "Automatic Model Orchestration"
  short_description: "Route Sol, Terra, Ultra, and Fast with durable controls"
  default_prompt: "Use $auto-orchestrate-task to plan, route, control, recover, review, and verify this substantial task."
policy:
  allow_implicit_invocation: true
