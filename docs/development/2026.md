---
status: active
owner: wei-er582
last_verified: 2026-07-14
verified_commit: "codex-auto-orchestrator@initial-implementation-2026-07-13"
applies_to: [codex-auto-orchestrator]
supersedes: []
---

# 2026 development log

## 2026-07-14 — Public repository publication

- Goal: publish the project with a clear public identity and enough documentation for external readers to evaluate and run it.
- Change: retained the discoverable name Codex Auto Orchestrator, rewrote the README as a complete product and technical introduction, changed active ownership metadata to `wei-er582`, and prepared GitHub description and topic metadata.
- Safety: scanned the current tree and complete Git history for credential, private-key, server-address, account-email, and oversized-blob patterns before changing visibility.
- Verification: 26 deterministic tests, documentation audit, plugin validation, Skill validation, Git whitespace checks, full-history sensitive-pattern scanning, and oversized-blob scanning passed. GitHub visibility was changed to `PUBLIC`; the repository description, eight discovery topics, public README, and Issues entrypoint were verified through GitHub.

## 2026-07-13 — Empty temporary directory cleanup

- Trigger: final residue inspection found empty per-job directories after snapshots and worktrees had already been removed correctly.
- Change: cleanup now removes an empty job directory and opportunistically removes the shared temporary parent, while preserving any non-empty evidence directory.
- Verification: all 26 tests passed; successful, parallel-write, and cancelled flows left no job temporary directory, while the changed-snapshot test still preserved its evidence until the test removed it.

## 2026-07-13 — Identifier-schema compatibility fix

- Trigger: a real implicit-trigger dry-run produced safe identifiers such as `wave_1` that passed the public output schema but failed stricter runtime validation.
- Change: added one shared lowercase identifier grammar to all structured schemas and runtime validation, documented it in the planner prompt, and accepted underscores.
- Verification: 26 deterministic tests passed, then a new task loaded installed version `0.1.1+codex.20260713151113` and completed implicit dry-run job `20260713T151241Z-cdeb28b5` on the first planner attempt with zero Workers.

## 2026-07-13 — Initial automatic model orchestrator

- Goal: implement an automatic plan, route, execute, monitor, review, and integrate workflow for Sol, Terra, and native Ultra.
- Background: native subagent invocation did not expose reliable per-worker model routing, while `codex exec` exposed model, config, structured output, and JSON event controls.
- Impact: adds a new local Codex automation capability; target project permissions remain bounded by the original task.
- Decision: package a Skill and dependency-free Python engine in a personal plugin; keep MCP and Hooks out of version 0.1.0.
- Changes: implemented model discovery, strict schemas, cross-process state and cancellation, routing, bounded escalation, Git worktrees, Windows read-snapshot isolation, per-task review and repair, session-backed model evidence, reporting, tests, and documentation.
- Data/configuration: runtime evidence is stored under `%USERPROFILE%\.codex\orchestrator\runs`; no production data migration.
- Verification: 24 deterministic tests pass; syntax, documentation, Skill, and plugin validators pass; real Direct, Ultra, and read-snapshot jobs passed with requested and observed model selections matched.
- Deployment: local plugin installation and GitHub/server revision proof are recorded in the release entry.
- Rollback: disable the managed trigger and reinstall the prior plugin revision.
- Remaining: none for version 0.1.0.
