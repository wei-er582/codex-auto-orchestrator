---
status: released
owner: Samsung
last_verified: 2026-07-13
verified_commit: "66437524e7e0509dc32f8752d7297b82c84569b6"
applies_to: [codex-auto-orchestrator]
supersedes: []
---

# Version 0.1.2 — 2026-07-13

## Revision

- Validated implementation: `66437524e7e0509dc32f8752d7297b82c84569b6`
- Release tag: `v0.1.2`
- Installed build: `0.1.2+codex.20260713152532`
- Distribution: personal marketplace `personal`, private GitHub repository `wei-er582/codex-auto-orchestrator`, and server mirror `/opt/codex-auto-orchestrator/current`

## Fix

Clean worktree and read-snapshot removal previously left an empty per-job directory under the shared temporary root. Cleanup now removes empty job directories and opportunistically removes the shared parent. Any non-empty directory remains preserved and blocks successful completion, so evidence safety is unchanged.

## Acceptance

- Deterministic suite: 26 tests passed.
- Targeted successful Direct, parallel-write, cancellation, and changed-snapshot tests passed.
- Successful and cancelled jobs left no per-job temporary directory, branch, worktree, or child process.
- The changed-snapshot failure still preserved its evidence directory until the test explicitly removed it.
- The complete test run finished with no `codex-auto-orchestrator` temporary root.
- Syntax, documentation, Skill, plugin, secret-pattern, and Git whitespace checks passed.
- Routing behavior and the verified implicit-trigger job `20260713T151241Z-cdeb28b5` are unchanged from version 0.1.1.

## Operational notes

- Empty-root deletion is path-guarded to the system temporary directory.
- Concurrent or intentionally preserved jobs prevent shared-parent deletion without failing another job.
- Runtime events remain outside temporary worktrees and are retained for at most 14 days and 20 jobs.
- The server copy is a revision mirror only and does not run a resident service.

## Rollback

Disable the global automatic trigger and reinstall tag `v0.1.1`. That version retains routing correctness but may leave empty per-job temporary directories that require manual cleanup.
